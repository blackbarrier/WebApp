<form method="post" action="{{ path('app_turno_delete', {'id': turno.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ turno.id) }}">
    <button class="btn">Delete</button>
</form>
